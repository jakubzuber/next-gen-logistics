import SideMenu from "../../SideMenu";
import { Container } from "../styled";

const Invoices = () => {

    return (
        <Container>
            <SideMenu />
            <p>Faktury</p>
        </Container>
    );
};

export default Invoices;