import SideMenu from "../../SideMenu"
import { Container } from "../styled";

const Releases = () => {

    return (
        <Container>
            <SideMenu />
        <p>Wydania</p>
        </Container>
    );
};

export default Releases;