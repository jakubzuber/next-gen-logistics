import SideMenu from "../../SideMenu";
import { Container } from "../styled";

const Clients = () => {

    return (
        <Container>
            <SideMenu />
            <p>Faktury</p>
        </Container>
    );
};

export default Clients;